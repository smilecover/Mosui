# 关键规则总结

| 原则 | 说明 |
|------|------|
| **单向无环** | 所有锚定引用必须形成 DAG（有向无环图），不能出现 A→B 同时 B→A |
| **内容驱动尺寸** | delegate 的 `implicitWidth/Height` 决定 Loader 在未锚定方向上的尺寸 |
| **锚定传递位置** | 边界坐标通过锚定单向传递：root → right → {up, down} → left → center |
| **叶子被动填充** | `__center` 四面锚定，是纯消费者，不向任何兄弟提供锚点 |
