# 2026-04-20 工作日志

## 路由 + Loader 页面显示
- `MosMenu.onClickMenu` → `Qt.resolvedUrl(data.source)` → `galleryRouter.push(url)`
- `Loader.source` 绑定 `galleryRouter.currentUrl`（不能用 `currentPath`，会丢 scheme）
- `Controls/*.qml` 已在 `Mosuiapp/CMakeLists.txt` glob 注册，无需额外配置


### Main.qml 结构 + MosIcon 未注册
- **问题1**：`Item`/`MosRouter` 误放进 `MosMenu{}` 内，导致 `anchors.top: captionbar.bottom` 报 "Cannot anchor to non-parent/sibling" → 把它们移出 `MosMenu {}` 外
- **问题2**：CMakeLists.txt `CPP_FILES` glob 未包含 `cpp/Icon/` → `MosIcon` 完全未编译进库 → QML 报 "MosIcon is not defined" → 修复：补加 `cpp/Icon/*.cpp` / `.h` glob 及 include 路径

---

## MosMenu 折叠箭头修复
- **问题1**：`anchors.top: captionbar.top` 导致 menu 从窗口顶部开始被标题栏遮挡 → 改为 `captionbar.bottom`
- **问题2**：`MosMenu.qml` 的 `__expandedIcon`（`MosIconText`）`iconSource` 被注释掉（因为 `MosIcon` 枚举为空），导致展开箭头不显示
  - 根因：`MosIcon.h` 的枚举类完全为空，`MosIcon.DownOutlined` = undefined
  - 修复：将 `__expandedIcon` 从 `MosIconText` 改为标准 `Text`，使用 Unicode `\u25BE`（▾），配合 `rotation` + `Behavior` 做平面旋转动画
- **问题3**：`menuButton.expandedVisible` 属性不存在（未声明）→ 改为 `menuButton.showExpanded`
