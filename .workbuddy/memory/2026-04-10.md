## 2026-04-10

### 代码审查 - MosUI 项目深度审查

完成了对 MosUI（Qt6 QML 桌面 UI 组件库）的完整代码审查，输出了详细审查报告。

**项目架构：**
- C++ 层：MosTheme（Token 主题引擎，Pimpl）、MosRectangle（自定义 PaintedItem）、MosWindowAgent（QWindowKit 集成）、MosColorGenerator、MosSystemThemeHelper
- QML 层：MosWindow、MosCaptionbar（空实现）、MosButton、MosShadow、MosText
- CMake 动态库，URI MosuiBasic，输出到 shared/

**主要问题清单（已在报告中详细记录）：**
- P0: MosApp 字体路径硬编码错误（`:/HuskarUI/...` 应为 `:/MosuiBasic/...`）
- P0: MosWindowAgent setup() 被调用两次（C++ classBegin + QML onCompleted）
- P0: MosRectangle::border() 使用 QQml_setParent_noEvent 私有 API
- P1: MosWindow.qml 使用未声明属性 showWhenReady
- P1: MOSUI_PROPERTY_READONLY 宏成员变量未初始化
- P1: initializeComponentPropertyHash 静态 initialized 标志多实例问题

**亮点：Token 表达式引擎设计成熟，Pimpl 正确使用，组件主题两级注册架构清晰。**
