# 2026-04-29

- 修复 CodeBox.qml 第 26 行报错 "Unable to assign [undefined] to QQuickItem*"：`rootWindow.captionBar` 应为 `rootWindow.captionbar`（全小写），与 MosWindow.qml 中 `property alias captionbar` 保持一致。
- 修复 CodeBox.qml 点击"运行代码"按钮无法打开 MosCodeRunner 窗口：① Window 类型组件 createObject 的 parent 必须为 null，不能是普通 Item；② 创建后需手动调用 show()；③ 增加 Component.Loading 异步回调处理。
- 修复跨模块 Qt.createComponent 路径问题：CodeBox（Mosuiapp 模块）中用相对路径 'MosCodeRunner.qml' 无法找到 MosuiBasic 模块的文件，status 为 Error 被静默跳过。改用 QRC 绝对路径 'qrc:/qt/qml/MosuiBasic/qml/MosCodeRunner.qml' 解决。
- 修复 MosCodeRunner 窗口特效不生效：子类定义的 Component.onCompleted 覆盖了父类 MosWindow 的 Component.onCompleted，导致 windowAgent 初始化、titleBar 设置等关键逻辑全丢失。修复：① effect 改用属性声明 `effect: MosWindow.Effect_dwm_blur`；② visible 改为属性声明 `visible: true`；③ 子类专属逻辑（setWindowStaysOnTopHint）改用 `onInitializedChanged` 信号处理，不再覆盖父类 onCompleted。
- 修复 MosCodeRunner 左下角白块：ScrollView 默认带白色背景，在 DWM 模糊窗口下显露。添加 `background: Item {}` 使其透明。
- 跨模块组件创建重构：CodeBox.qml 中用 `Qt.createComponent('qrc:/...')` 硬编码路径不可跨平台。改为 `Component { id: codeRunnerComponent; MosCodeRunner {} }` 内联声明，配合 `import MosuiBasic` 通过模块类型解析，无需任何路径。
- 完成全项目代码审查：审阅了 MosWindow/MosButton/MosRadioBlock/MosCaptionbar/SettingsPage/MosCodeRunner/Mostheme.cpp 等核心文件，输出 P0~P2 共 10 项改进建议，涵盖 setEffect 重复代码、parse() 巨 switch-case 开闭原则违反、Ripple 动画重复、循环引用检测缺失、平台判断硬编码、anchor 冗余等。
- **P0 三项重构已全部完成**：
  1. MosWindow.setEffect()：65 行 switch-case → 映射表 + 循环，25 行搞定；同时删除 captionbar 多余的 `width: root.width` 冗余 binding
  2. 抽取 MosRippleEffect.qml：从 MosButton/MosRadioBlock 中提取完全相同的涟漪动画为独立组件，两处各减少 ~40 行
  3. Mostheme.cpp parse()：190 行 switch-case + Function 枚举 → 函数注册表（QHash<QString, TokenFunc>），parse() 缩减为 20 行纯分发器；删除 Function 枚举，新增主题函数只需在 initializeFuncRegistry() 注册一个 lambda
